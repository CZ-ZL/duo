# DUO 今晚交付报告（2026-09-13）

代码已上传到 `CZ-ZL/duo` 私有仓库，版本定位为实验性 **0.3.0 source
preview**。没有把产品功能验证、实验运行完成、方法优势成立混为一谈。
GitHub CI 的实时状态请看仓库 Actions；本文件不预先声称远程检查通过。

## 实验实际完成到哪里

本次最新的配置 headroom 实验已完整结束：3 种配置、2 轮、6 个真实
DSH 会话，共 12 次模型请求，没有失败、非法输出或重试。

| 内容保留上限 | 第一轮 | 第二轮 |
|---|---:|---:|
| 原厂默认 100,000 字符 | 3/6 | 3/6 |
| 压缩至 50,000 字符 | 2/6 | 2/6 |
| 扩大至 200,000 字符 | 6/6 | 6/6 |

结论：这个实际配置 Target 有可测量的质量改善空间。它使用固定文档
快照传输，原生内容保留、模型和工具执行是真实的；公网 HTTP 并未在
本实验中验收。两轮数据只是有界诊断，不能代表所有任务中的稳定收益。

本包费用 **¥0.048678680**，来自真实 usage 与冻结人民币费率。所有
12 条费用回执核对一致，1026 条冻结文件哈希核对未改变。原 ¥10 批次
累计为 **258 次／¥2.240218893**，余额 **¥7.759781107**。本包未用
请求槽位已经释放，没有自动挪给其他研究；旧失败与旧费用继续保留。

更早的三代四臂正式实验也已经完整结束：B0/B1/B2/B3 分别为
9/18、8/18、8/18、8/18，优化臂都保留了原版。正式结论仍是：
**没有证明 DUO 相比合理单环具有质量或 API 费用优势。**
后续 shadow final 与 Selection Audit 的全部分数、失败和限制见
[实验报告](EXPERIMENTS.md)及[机器可读证据索引](experiment-evidence.json)。

## 代码和发布整理

- 复用已经验收的 DSH/Cordis 原生 persona 插件，保留真实预算、Journal、
  Slow 反馈、组件替换、warm start 和恢复能力；没有重写架构。
- 复用已完成的精确人民币计价修复、包依赖/导出清理、默认示例实际运行、
  独立文档输入和产品/研究测试边界整理。
- 本轮补上公开实验报告、聚合结果与原始报告哈希、BigCodeBench 样例的
  Apache-2.0 许可证及来源说明，并清理一处测试文件末尾空行。
- 上传独立的源代码仓库，排除凭据、用户配置、原始运行目录和账号账本。
  保留的旧基准回归 fixture 全部标为已暴露数据，不能作为 unseen final。

## 验证证据

此前同一运行时代码的完整产品 gate：**285 项原生测试、450 项 Python
产品测试、21 个 DSH 宿主场景通过**；另有 **86 项研究测试被明确排除**，
不能算作通过。它们需要原研究档案，源码包不是完整研究复现档案。

本轮新增实际验证：新 profile 中 `dsh plugin add` 离线安装成功，50 个
安装文件与 tarball 源文件一致，bundle 自动注册；使用已有宿主依赖的
新 packaged CLI 演示成功。清理空行后，受影响原生测试 1/1 通过。
独立只读审查没有发现严重的代码/打包缺陷，最终提交差异检查通过。

本地 npm registry 探测遇到 `ECONNRESET`，因此本地离线安装不冒充
全新机器的联网安装。GitHub 工作流从新环境安装依赖并运行产品 gate，
其远程结果另行核验。TypeScript 语义编译仍未验收，不作保证。

## 仍然没有完成的部分

**新的配置 Target 的 B0/B1/B2 搜索和独立候选 final 尚未运行。**
默认产品现在仍支持 persona Target；通用服务替换接口存在，但这不等于
配置 Target 的生成、执行、去重和历史适配器已经实现。最新 headroom
变体是预先固定的配置，并非 DUO 自动生成的候选。

所以更大的 Root Cause Goal 仍未关闭，Generator/Search 与
Evaluator/Selection 的因果归因仍是 **INSUFFICIENT_EVIDENCE**。
这份交付可以作为实验性项目源码和已完成实验的记录，不能宣称全部
研究目标、任意 Target 支持或正式 1.0.0 发布要求均已满足。

下一项研究工作是完成最小配置 Target 适配器，冻结新的候选搜索、独立
final 数据和有限预算，再做 B0/B1/B2。当前没有启动 Graph/Bayesian、
没有自动采用候选，也没有自动扩大模型预算。
