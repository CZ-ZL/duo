# Bounded 0.6.1 product release closeout

Follow-up: the record below is the original local freeze. The later audit found
F01; its [repair and new artifact](F01_REPAIR.md) supersede this package for
delivery. Original failures, checksums and results below remain historical.

Status: LOCAL_SCOPE_VERIFIED_AND_FROZEN; fresh registry installation BLOCKED. The owner approved a finite release pass after the second
independent Caller trial. Previous product/research Goals and evidence stay sealed.
0.6.1 is a local candidate until supported checks and a matching delivery receipt
exist; this document does not claim a GitHub upload or npm publication.

| Item | Required result | State |
|---|---|---|
| C1 Preparation resources | All four returned evaluator/example paths exist in the actual tarball; fixed controls run from an isolated installed profile | COMPLETE: installed public checks 63 PASS /85 steps, including 4 fixed controls; native 333 PASS |
| C2 Bounded guidance | Legal CNY0 configuration and actual warm history role/use/source/delta fields documented; no change to monetary or history policy | COMPLETE: public schema, Agent Guide, Providers and shipped guide |
| C3 Release regression | Same frozen build passes the existing product gate plus installed preparation/control checks; fixed public example task, no new business search | Supported local path PASS. Full fresh-registry gate BLOCKED by ERR_PNPM_META_FETCH_FAIL; not declared fully passed |
| C4 Frozen delivery | Exact package checksum, supported boundaries, known limitations, test receipts and handoff retained | LOCAL COMPLETE: 0.6.1, SHA256 5fe28bdf5d041e6c2c955afde2737b04dc25610d2909663f8d68e4eff6e8ebf4; GitHub/npm publication NOT_RUN |

Exit when these scoped checks pass. Only broken installation/primary public paths,
incorrect measurements/selection, budget or permission violations, and lost durable
evidence block release. New task-specific integrations and optional UX improvements
go to backlog. A new acceptance business is not required to finish this version.

Changes: reuse the shipped function-evaluator/control wrapper and actual local text
measurement; add only the missing package control adapter/contract/profile assets,
repair stale onboarding references, and document existing CNY0/warm boundaries.
The preceding valid-but-infeasible baseline repair remains included. No new core
service, evaluation algorithm, benchmark, provider research or loop is introduced.

The installed control case exposed two bounded fixes: required descriptor fields
were missing in the new example, and evaluator matching incorrectly rejected
explicit control-purpose evidence in evaluate-only mode. Control admission now
permits evaluate-only non-final stages; optimization and final continue to reject
control data. Existing budget, score and selection rules are unchanged.

Python regression: the initial full product run recorded 449 PASS, 2 FAIL, 86
research tests deselected. Both failures were offline Caller fixture input-limit
stops at 56,108 bytes versus 52 KiB. The fixture now declares 64 KiB and matching
simulated reservation before execution; all four configured-Caller cases pass on
retest. Production defaults and request admission were not expanded. The other
449 passing results are reused; this is not a second full-suite pass claim.

Other completed evidence: actual DSH host 21 profiles, Slow evidence 31 checks,
baseline repair 5 cases, sandbox control, format/types/docs/package checks. Those
unaffected passes preceding the final control-purpose fix remain separately
identified in the local evidence report. Earlier independent Caller acceptance
is also reused, not rerun with a new business.

Known limitations: trusted in-process providers; Linux/Node24 tested environment;
settled unchanged-checkpoint recovery only; custom adapters need caller code;
optional cumulative CNY cap accepts positive values only (local runs keep per-run
zero and paid:false); public history has no candidate-private family field;
Caller inference is outside native accounting. No automatic adoption or method
superiority claim. Unsupported OS, unsettled recovery and arbitrary no-code Target
integration are not silently promoted to supported.

Local evidence root: `dualloop/runs/product-release-closeout-20260915/` in the
authorized workspace. Independent Caller round-2 evidence precedes these narrow
packaging/docs changes and is reused only for unchanged runtime behavior; it is
not called a new Agent trial of 0.6.1. The final package task is scripted actual
DSH public-interface acceptance, not autonomous Agent judgment.
